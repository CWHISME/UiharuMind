# 代理人格化：落地进度与待议

> 一句话：**群聊方案经过两轮对照代码的会审，结论落成 ADR 0043-0046（实现期又补 0047）；0044、
> 0047 已全部实现，0043 阶段 1-2、0046 群聊骨架已落地，还没在真模型上跑过 Hello World。**
>
> 本文是**接着干的入口**，不是决策记录——决策在 `docs/adr/`，方案（v6）与人格稿（v8）在同目录。

## 下次从这里开始

**闸门是 Hello World**（步骤见下方「未决清单」第 3 条）。它回答的是「人格方向对不对」，
方案 §8 的 P0 余项与全部 P1 机制都排在它后面——不像人，那些做了也白做。

**跑之前先做一件小事（推荐）：让「谁在发言」看得见。** 现在群视图只在成员说完时冒出一条，
智能体读文件那一轮可能要几分钟，期间只有一个转圈——看 Hello World 正是要盯着这段。
做法：调度器把当前发言人报出来（`GroupChatCoordinator` 里已经有 `_speakers`），
输入区的忙碌文案显示「初春 正在发言…」，右栏成员列表标出正在说的那一位。约半小时，不碰存储。

> **✅ 已完成（本轮）**：`GroupChatCoordinator` 暴露 `CurrentSpeakerOf` + `SpeakerChanged` 事件；
> 忙碌文案显示「{成员} 正在发言…」（空窗退到「群聊进行中…」）；右栏成员列表给正在说的那位
> 挂蓝点（`GroupMemberItem.IsSpeaking`，装载时补刷一次）。两条新测试钉住事件行为（轮到/结束各通报、
> 全员无新话不通报）。顺带做的两件会审尾巴：**agent 不发开场白补进创建入口**（`ChatSession` 带角色构造
> 与 `ConversationSessionBinder.CreateAsync`，与编辑页藏字段同口径），以及两处注释硬违规
> （`SubAgentTool.AnonymousCharacterOf` 双 summary、三处指向 `SubAgentDefault` 的悬空 cref）。
>
> **再补一块 Hello World 前置：基座层落地**（人格稿 §0 的「基本准则 / 三条红线 / 执行义务 / 讨论立场」）。
> 之前系统里没有它——手建角色要手动把 §0 贴三遍，还容易漏。现在 `AgentBasePrompts` 在
> `AgentInstructionsComposer` 恒注入 agent 档主代理系统提示的最前（`# 基座`，独立段，能力面板可见），
> 子代理与普通角色不注入（子代理是任务书；普通角色无工具，基座里的「查文件/落盘」是没法兑现的指令）。
> 一条新测试钉住「基座恒在、且在人格之前」。内置卡已落地 `src/UiharuMind.Core/Resources/Cards/Agents/`
> （魔禁班底 + 五位候选），改人格直接改 `.md`，零代码。
>
> （当时口径）编译 0 错 0 警，测试 Core 981 + App 465 全过；HEAD 数字见文末状态行。

> **SendMessage 参数修复（实测反馈）**：`to` 曾无默认值，被 schema 标成必填——模型不传直接报
> missing required、被迫瞎填 "default" 又被拒。已给 `to` 加 `""` 默认值（挪到 `content` 之后，
> C# 可选参数规则），不传即落默认对象；"default" 刻意<b>不</b>宽容（它不是合法收件人，
> 填了该报错让模型学会留空）。`NormalizeTo` + schema 两条测试钉住，见 `SubAgentToolSendMessageTests`。

**Hello World 之后按结果分叉：**

- **不过关** → 只改人格稿（`角色提示词_v8.md`，并同步改对应的内置卡 `.md`），重跑。**不写代码、不加机制**——
  方案 §7 的调人格顺序：先硬禁令，再「怎么说话」，最后才动「在乎什么」
- **过关** → 按下面的顺序往下做

| 顺序 | 做什么 | 为什么排这儿 | 量级 |
|---|---|---|---|
| ~~1~~ | **魔禁四人做成内置卡** → **已完成（ADR 0047）**：内置卡改 markdown + 目录扫描、枚举缩为代码点名；魔禁班底与五位候选都已成卡 | 改卡 = 改 `.md`，「过了关才固化」的理由已消失 | — |
| 2 | **方案 §8 的 P0 余项**：成本可见（每位成员累计 token，放右栏成员列表）、本群产物区（群工作区里的文件，带路径）、Conclude（手动结题 → 群只读归档） | 骨架刻意没做的 P0；产物区是 §1.4「产出锚点」的落地，Hello World 的第三件事「盘上有没有东西」正要靠它看 | 中 |
| 3 | **并行调度 + 唤醒边界**（方案 §3.2、§6.1；ADR 0046 未决第一条） | Hello World 的题目就是它，**那 10 圈的会审结论直接当这一步的输入**。投递两种都已做好，这一步只改调度 | 中 |
| 4 | **成员审批接进群视图**（ADR 0046 未决第三条） | 骨架里一律拒绝；成员一旦要跑 shell 就卡住。要先定：审批卡画在群视图里，还是跳到那位成员的会话窗口 | 中 |
| 5 | **成员会话私聊**（ADR 0046 未决第二条） | 要先定私聊轮与群那一轮的并发口径：同一个成员同时有两轮，「最后正文」会被私聊的回复顶掉 | 中 |
| 6 | **单聊拉人 / 单聊升级成群**（方案 §6.6b，P1） | 依赖 3（谁唤醒谁）与群的类型规则（普通单聊里 @ 智能体怎么办，见第二轮会审 Q5 的压力场景） | 中 |

**随时可插的小项（不挡主线）：**

- **Core 测试隔离数据目录**：App 测试有 `AppDataDirectoryInitializer`，Core 测试没有。目前没有测试写会话，
  但下一个在测试里 `SessionManager.Instance.Add` 的人会直接写进用户的 `~/.uiharu`。照抄那个模块初始化器即可
- **场景说明挪进系统提示的场景层**：现在随首次投递放在成员历史开头，压缩裁掉开头之后就没了。
  群聊变长之前再做，要同时看住装配快照（改系统提示会触发重建）
- **`Lang.Designer.cs` 去留**：~~它是 IDE 生成、早已不随 resx 同步（新键都不在里面），
  但截图那几处（`ScreenCaptureWindow`、`CaptureMagnifier`）还在读 `Lang.X`。先把这几处迁到 `LangKey`，再删掉它~~
  **已完成**（`a89bbc7c` 退役为手写最小 `Lang` 类，残留 `Lang.X` 全部迁 `Loc.Text(LangKey)`）
- **群在界面上的几处占位感**：左栏群条目用的是默认头像；右栏顶部工作区卡片写着占位角色的信息
- **没验证的一条**：在单向锁加上之前（`78fff4a6` 到本轮之间）就被翻回普通角色、名下带会话的智能体，
  它的老会话会把工具调用历史在不挂工具定义的请求里发出去，provider 会不会报错没测过

**候选班底（新增，不在主线）**：`候选角色提示词.md`（白井黑子 / 夜神月 / 千空 / 鲁路修 / 凤凰院凶真）
已全部做成内置卡（`ShiraiKurokoAgent` / `YagamiLightAgent` / `SenkuAgent` / `LelouchAgent` /
`HououinKyoumaAgent`），未进代码点名班底。文件写明「可直接丢进 Hello World 对比」——先魔禁三人过关，
候选按需再比，别把闸门题目搞混。

## 2026-09-23 第二轮会审：已定

| 议题 | 结论 | 落在 |
|---|---|---|
| 编辑页开关去留（原 A/B） | **A，留在编辑页**，改叫「启用智能体能力」、作能力卡片的总开关 | ADR 0043「已定」 |
| 合并后那一类的叫法 | **「普通角色」**（英文 `Chat`），不是「角色」——那是三类的总称 | ADR 0043「已定」 |
| 术语表 | **现在就改**：旧词条说的东西已不存在（四档、`RunSubAgent`、探索档）。新词仍等实现落地 | `docs/CONTEXT.md` 已改 |
| 接下来的顺序 | **阶段 2 → 群聊骨架 → 阶段 3 等真有需要** | — |
| 群的类型 | **建群时选定、不变**；智能体群绑工作区可混装，普通群只收普通角色 | ADR 0042「已定」 |
| 身份开关怎么翻 | **单向**：普通角色 → 智能体随时可翻；反方向只在名下无任何会话时可翻。子会话装配处另设防线 | ADR 0043「已定」 |
| Hello World 形态 | **不做一次性原型，直接做骨架**，骨架上跑 Hello World；**P1 机制等它通过再开** | — |
| 群发言存哪儿 | **投递进成员会话**（副本方案），群壳会话只记给人看的流水；0045 的否决理由不成立 | **ADR 0046**（取代 0045） |
| 成员怎么挂到群 | `GroupId`，**不复用 `ParentSessionId`**（会按子代理装配） | ADR 0046 决策 2 |
| 什么算群发言 | 调过 `SendMessage` 发群的以它为准，否则最后正文就是发言 | ADR 0046 决策 4 |
| 调度 | **先串行**，一圈即停 + 「继续一圈」；投递两种都做（闲时合成一条、跑时走注入队列） | ADR 0046 决策 3、5 |
| 群发言能不能改 | **送达即不可改** | ADR 0046 决策 6 |
| 验收题目 | 「并行调度时谁唤醒谁、什么时候停」——原题（黑板裁剪）在副本方案下已不存在 | — |

**本轮核实的事实**（翻开关的后果，均无测试覆盖）：

- 会话不存类型，`SessionManager.KindOf`（阶段 2 后为 `CharacterOf`）读角色**当前**的 `IsAgent` → 翻开关，名下老会话整体换侧
- 智能体 → 普通角色：历史里的工具调用原样发出、`Tools` 为空（`SessionChatHistoryProvider.cs:60-85`
  不过滤）。provider 侧会不会报错**未验证**
- 普通角色 → 智能体：老会话无工作区，落共享 `Cache/Scratch`（与没绑工作区的智能体会话同一条路）
- **已翻成普通角色的角色，其老子会话会装回残留工具**：`AgentAssembler.cs:47` 只看 `profile.SubAgent`，
  不查身份；`Tools` 字段翻转不清空 → 挂 shell、工作目录为空串、能力交集丢失（**已修**）
- 编辑页无确认；翻完列表不立即刷新（角色保存不触发会话事件）（**已修**：单向锁 + 订阅 `OnCharacterUpdated`）
- 过期引用：`BuildRoleplayOptions`（`CharacterData.cs:29`、`CharacterKindPresentation.cs:19` 注释）、
  `Harness/README.md:32` 的 `AgentHost` 与 `RoleplayZeroInjectionTests`

### 群聊骨架的范围

- **做**：建群（类型 + 成员 + 智能体群的工作区 + 名字）、群壳流水、成员会话、用户发言、串行一圈 +
  「继续一圈」、停止、按发言人渲染
- **不做**：私聊（成员会话只读）、@ 路由与唤醒规则、主持人点名、产物区面板、Conclude、单聊升级成群、
  入群上下文策略、并行调度
- **动手前待确认的默认**（没人反对就按这个）：
  - Hello World 三位（婚后光子 / 一方通行 / 初春）**直接用内置卡**（`KongoMitsukoAgent` /
    `AcceleratorAgent` / `UiharuKazariAgent`）；人格不过关就改卡的 `.md`，与人格稿同步
  - 三位都开智能体能力，建一个**智能体群**，工作区指向本仓库（开场读文件、初春落盘都要工具）
  - 发言顺序 = 建群时选成员的顺序
  - 成员会话在群的右栏列出，点开是**只读**的会话窗口（复用子会话浮窗）

## 已落地（4 个提交）

| 提交 | 内容 |
|---|---|
| `33eb4bb0` | 会审结论落成 ADR 0043-0045 + v5 两份提案稿修订为 v6 |
| `6b9b2a28` | **ADR 0044 全部实现**：委派门面统一为 `SendMessage` |
| `7586e384` | **ADR 0043 阶段 1**：身份轴四档枚举 → `IsAgent` 一轴 |
| `78fff4a6` | 新建角色不再先选档 |
| `91287473` | **ADR 0043 阶段 2 + ADR 0046 群聊骨架**：身份轴收尾（单向锁 / 子会话防线 / 翻完即刷列表）、群壳流水 + 成员会话投递 |
| `6e8e95c6` | LocExtension AOT 化（去反射、拆文件） |
| `a89bbc7c` | Lang.Designer 退役为手写最小 Lang 类，残留 `Lang.X` 全迁 `Loc.Text(LangKey)` |
| `2ed95340` | **ADR 0047**：内置卡改 markdown + 目录扫描、枚举缩为代码点名；连同基座层与配套归档 |
| `d9b9e184` | 基座提示落地 |
| `821a23ca` / `41b23292` | 提示词更新（webfetch 支持下载文件 等） |
| `394d3477` | 新增五位候选角色卡（白井黑子 / 夜神月 / 千空 / 鲁路修 / 凤凰院凶真） |
| `fa4f53eb` / `0487ab40` | 头像统一为单套并修缩放锯齿，晨曦 / 初春 / 白井黑子换图 |

状态：**编译 0 错 0 警；测试数字待技能线（自建 `load_skill` 顶替）落定后重跑核实**。
跑法按仓库口径 `dotnet msbuild <csproj> -t:Test`，**不要用 `dotnet test`**（测不出结果）。

### ADR 0044 做了什么

三把工具 `RunAgent` / `RunReadOnlyAgent` / `ContinueAgent` → **一把 `SendMessage(to, content, role?, model?)`**。
`to` 自己分流：人名 → 新开一次；`[sub-session: …]` 编号 → 续上那一次；留空 → 默认对象。
收件人名单从**工具 description** 挪进**系统提示的委派一节**（`ToolDisciplineFacts.DelegationRoster`），
于是工具定义在一次会话内恒定不变。政策正文整段改写成对话心智（不再有 run / task / 派活 / 选哪一档）。

**骨架一行没动**：`ParentSessionId`、后续报告、唤醒轮、`SessionRunRegistry` 原样。

存量按**就地封存**处理（不是退役）：`ESubAgentType`、`SubAgentProfile.Explorer`、
`ExploreSubAgent` 卡、`ExplorerSubAgentModelName` 全部保留并在注释里标明「仅为重建存量」——
真删会让老的只读子会话重建后**变成可写**、或者**打不开**。只真删了设置页那个已经不起作用的选择器。

### ADR 0043 阶段 1 做了什么

`CharacterData` 的存储轴换成 `IsAgent` + `IsUserCard` 两个 bool；
`Kind` 降级为 `[JsonIgnore]` 的**派生只读投影**（永不产出 `Tool`）。

> **这个投影是 churn 小的关键**：约 35 个读取点一行没动，编译器只报出了写入点（生产代码只有 4 处）。

- 扮演与工具人在徽章、筛选、排序、头像、编辑表单五处合并
- 16 张内置卡的 JSON 就地改写
- 翻译下拉判据换成「不是 agent、不是用户卡、**没有开场白**、**没开注入用户卡**」
  ——把档位这个代理指标换成它本来在代理的那个属性
- 迁移靠 `CharacterData.LegacyKind`：`[JsonPropertyName("Kind")]` 的**只写属性**，
  老存档的 `"Kind": "Agent"` 进来即映射到新轴，只有 setter 所以不参与序列化。
  **为什么需要**：`SaveDefaultCharacterDataPath` 下可能有用户改过的内置卡副本，那些文件不在仓库里。
  有测试钉住往返与迁移两头（`IdentityAxis_RoundTrips_AndLegacyKindStillMigrates`）

## 本轮推翻了 v5 的三处结论（都基于事实错误）

会审的价值主要在这里，别把它们当成"又改主意了"：

1. **§2.6③ 删权限档** → **撤销**。两个事实错误：现存权限档不是"预先拦"而是"问一句"
   （`ApprovalModeMapper` 只产出自动放行规则，工具照样挂载）；而它要接手的**撤销栈根本不存在**
   （只有 `FileBackupStore`，无账本、无回滚接口、备份失败不抛异常）。
   净效果是删掉一道在工作的护栏、换上一道不存在的
2. **§6.8 工具全量共享** → **撤销**。论据「增删工具会让所有共享该前缀的会话失效」是事实错误：
   **角色之间本来就不共享前缀**。而且删掉 `AgentToolConfig` 会让陪聊角色挂着 shell
3. **§3.3 隐式 Yield** → **不是待设计项**，现架构已实现（`HasPendingWork` + 唤醒轮），
   而且比方案写得好——方案那句「控制权留调度器」正是术语表警告过的坑

## 未决清单

**已定但没做**（按顺序）

1. ~~**ADR 0043 阶段 2**~~ **已完成**：`ECharacterKind` 与 `Kind` 投影删除，调用点直读
   `IsAgent` / `IsUserCard`，组合判据留在 `CharacterKindRouting`（`IsChat()` / `CanStartSession()`）。
   编译 0 错 0 警；测试 952 + 461 全过。顺带做掉的：
   - **开关单向**（`CharacterDraft.IsAgentLocked`，数会话用 `SessionManager.CountSessionsOf`）+
     **子会话装配防线**（`AgentAssembler`：子会话且仍是智能体才走子代理装配，有测试）+
     **翻完即刷列表**（`SessionListModel` 订阅 `OnCharacterUpdated`）
   - 编辑页：「启用智能体能力」总开关，锁住时原因留在行上；「注入用户卡」挪进提示词卡片
   - 多语言：`CharacterKindRoleplay` → `CharacterKindChat`（普通角色 / Chat）；删
     `CharacterKindTool` / `CharacterFuncType` / `AgentSettingExplorerSubAgentModel(Desc)`；
     子代理那三条文案里的 `RunSubAgent` 与「只读、不存档」的过时说法一并改掉
   - `LegacyKind` 垫片改收 `JsonElement`：`SaveUtility.LoadFromString` 遇异常会返回一张**全新空白卡**
     （连 `CharacterId` 都换了），非字符串的旧值不能让它抛
   - 过期引用：`BuildRoleplayOptions`、`Harness/README.md` 的 `AgentHost` / `RoleplayZeroInjectionTests`、
     注释里的「角色扮演档 / 智能体档」与现行口吻的 `RunAgent` / `ContinueAgent`
2. ~~**ADR 0046 群聊骨架**~~ **已完成**。Core 在 `AI/Chat/Group/`，界面在
   `Features/Conversation/Group/`；编译 0 错 0 警，测试 971 + 461 全过（新增 19 条调度与投递测试，
   跑模型的那段换成假的）。群视图用无头渲染截图核过：按发言人画头像、输入区只剩发送与「继续一圈」。
   落地时定下的细节记在 ADR 0046「骨架做了什么」（场景说明随首次投递、成员审批一律拒绝、工作区以群壳为准）。
   **没验证的**：真模型跑一圈的效果——这正是下一步
3. **在骨架上跑 Hello World**（需要你来）：选内置卡婚后光子 / 一方通行 / 初春（`KongoMitsukoAgent` /
   `AcceleratorAgent` / `UiharuKazariAgent`，卡上正文即人格稿 v8 §1、§2、§4）；
   在智能体一侧选好工作区（本仓库）→ 空态点「创建群聊」→ 按顺序勾三位 →
   题目「并行调度时谁唤醒谁、什么时候停」→ 每圈之后点「继续一圈」，共 10 圈。
   按人格稿 §7 看三件事：像不像人、挑刺重不重叠、盘上有没有东西。**不过关就改人格稿，不加机制**
4. **ADR 0043 阶段 3**：用户卡迁出。**等有具体需求再开**，群聊不依赖它

**明确搁置的（不是忘了）**

- **`open_issues` 与 §6.2 的两个收敛信号**。重开时**先答「谁产出」再动手**——
  默认最省事的那条路（让初春维护）正是 §2.4 亲手砍掉的控制量
- **群成员开子代理**（v5 §6.4）：群成员是 peer 在第 0 层，他们开的子代理在第 1 层，
  **不撞**现有的"不许递归"不变量。可做，优先级最低
- **`CreateAgent` 原语**：不做。临时职责用 `SendMessage` 的 `role` 参数；持久角色由用户建。
  仓库里有现成证据——`SubAgentTool.Create` 的注释记着「曾经有过自定义子代理提示词参数，
  实测本地模型往里填的是泛泛套话」，那就是 `CreateAgent` 的缩小版实验，结论是删掉

**没写进术语表，是刻意的**

没落地的词（开场读取、主持人点名、产物区）写进去术语表就开始说谎，**等实现落地再补**。
反过来，**已落地的改动要当场改术语表**——已补身份轴（`IsAgent` / 普通角色）、委派（`SendMessage`），
以及群聊骨架的三个词（群壳会话 / 群成员会话 / 群发言）。「黑板」一词随 0045 作废，不进术语表。
ADR 0047 落地后「代码点名卡 / 内容卡 / BuiltInCharacterId」已补进 CONTEXT.md——本文此前没跟上，补记。

## 给接手者的几个坑

- **`dotnet test` 在本仓测不出结果**（0 通过不代表测试真过了）。判成败看
  `dotnet msbuild <csproj> -t:Test` 的输出，且要核对「总计」不是 0
- 政策提示词正文里**反引号只能包工具名**，有不变量测试钉着
  （`MainAgentInstructions_BacktickNamesMustBeKnownTools`）。给参数名加反引号会红
- 提交信息**只写一句话**，无正文、无尾注
